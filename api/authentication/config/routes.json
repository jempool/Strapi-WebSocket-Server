{
  "routes": [
    {
      "method": "POST",
      "path": "/auth/login",
      "handler": "authentication.login",
      "config": {
        "config": {
          "controllers": ["api::authentication::authentication"]
        }
      }
    },
    {
      "method": "POST",
      "path": "/auth/signup",
      "handler": "authentication.signup",
      "config": {
        "config": {
          "controllers": ["api::authentication::authentication"]
        }
      }
    }
  ]
}
